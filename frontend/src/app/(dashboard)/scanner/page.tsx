import { ScannerView } from "@/components/features/scanner/scanner-view";

export const metadata = {
  title: "Market Scanner | StockPilot",
  description: "AI-powered stock discovery with horizon and risk filters, live metrics, and confidence scores.",
};

export default function ScannerPage() {
  return <ScannerView />;
}
